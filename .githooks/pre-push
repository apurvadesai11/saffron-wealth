#!/bin/sh
# Runs before every `git push`. Skippable with `git push --no-verify`.
set -e

echo "pre-push: lint"
npm run lint

echo "pre-push: typecheck"
npm run typecheck

echo "pre-push: unit tests"
npm test
